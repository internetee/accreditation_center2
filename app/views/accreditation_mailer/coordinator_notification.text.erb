Accreditation Expiry Alert

Dear Coordinator,

📋 ALERT: <%= @expiring_users.count %> user(s) have expiring accreditations

The following users have accreditations that are expiring soon and may need attention:

<% @expiring_users.each do |user| %>
- <%= user.username %> (<%= user.email %>)
  Registrar: <%= user.registrar_name %>
  Expiry Date: <%= user.accreditation_expiry_date&.strftime('%B %d, %Y') %>
  Days Until Expiry: <%= user.days_until_accreditation_expiry %>

<% end %>

RECOMMENDED ACTIONS:
- Review the list of users with expiring accreditations
- Contact users who haven't started their renewal process
- Monitor test completion rates
- Follow up on any issues or concerns

You can access the admin dashboard to view detailed information about these users and their test attempts.

---
This is an automated alert from the Accreditation Center system.
Generated on <%= Time.current.strftime('%B %d, %Y at %I:%M %p') %>
