<div class="menu--mobile">
    <button type="button" class="button button--menu" data-menu-toggle="">
        <i aria-hidden="true" class="fas fa-times"></i>
    </button>
    <%= render 'common/menu_language' %>
    <%= render 'common/menu_user' %>
    <% if current_user %>
        <%= render 'common/menu_main' %>
    <% end %>
    <nav class="menu--portal">
        <a href="https://internet.ee"><span><%= t('common.header.open_portal') %></span></a>
        <a href="<%= ENV['registrar_portal_url'] %>" target="_blank"><span><%= t('common.header.registrar_portal') %></span></a>
    </nav>
</div>