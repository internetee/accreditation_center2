<nav class="menu--user">
    <% if current_user %>
        <strong><%= link_to "<i class='fas fa-user'></i>#{current_user.username}".html_safe, '#' %></strong>
        <%= link_to t('devise.sessions.sign_out'), logout_url, method: :delete, data: { turbo: false } %>
    <% else %>
        <%= link_to t('devise.sessions.sign_in'), login_url %>
    <% end %>
</nav>